export default theme => ({
  closeButton: {
    position: 'absolute',
    right: theme.spacing(1),
    top: theme.spacing(1),
    color: theme.palette.grey[500],
  },
  buttonSave: {
    textTransform: 'initial',
    fontWeight: 'bold',
    margin: '5px',
  },
})
