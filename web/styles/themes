export const orig = {
  accentColor: 'auto' /* Default color for inputs */,
  colors: {
    brand: 'black',
  },
  fontFamily: 'Montserrat, sans-serif',

}

const blue = '#141953'
const blueFeurst = 'hsl(210.6, 50.9%, 41.6%)'

export const feurst = {
  accentColor: blueFeurst /* Default color for inputs */,
  colors: {
    brand: blueFeurst,
    blue: blue,
    blueFeurst: blueFeurst, // Logo
    white: '#FFF', // pure white
    redAlert: 'red',
    yellow: '#e0ba14',
    black: '#111',
    metalGray: '#a8a9ab',
    lightGray: '#e4e4e4',
  },
  containerSize: {
    base: 'min(100% - 2rem, 60rem)',
    lg: 'min(100% - 2rem, 70rem)',
    xl: 'min(100% - 2rem, 80rem)',
  },
  fontSizes: {
    xxs: '0.6rem',
    xs: '0.75rem',
    sm: '0.875rem',
    base: '1rem',
    lg: '1.125rem',
  },
  fontFamily: `Roboto, 'Source Sans Pro', sans-serif`,
  lineHeights: {
    normal: 1,
    title: 1.25,
    paragraph: 1.5,
  },
}

const redaftral = '#a13849'

export const aftral = {
  accentColor: redaftral,
  colors: {
    brand: redaftral,
  },
  fontFamily: `Poppins, 'sans-serif'`,
}
