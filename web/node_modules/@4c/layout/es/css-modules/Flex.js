function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

function _objectWithoutPropertiesLoose(source, excluded) { if (source == null) return {}; var target = {}; var sourceKeys = Object.keys(source); var key, i; for (i = 0; i < sourceKeys.length; i++) { key = sourceKeys[i]; if (excluded.indexOf(key) >= 0) continue; target[key] = source[key]; } return target; }

/* eslint-disable react/prop-types */
import classNames from 'classnames';
import React from 'react';
import { propTypes, defaultProps } from '../PropTypes';
import camelCase from './camelCase';
import styles from './Layout.module.css';

function Flex(_ref, ref) {
  var Component = _ref.as,
      direction = _ref.direction,
      reverse = _ref.reverse,
      pad = _ref.pad,
      wrap = _ref.wrap,
      grow = _ref.grow,
      inline = _ref.inline,
      flex = _ref.flex,
      align = _ref.align,
      alignSelf = _ref.alignSelf,
      alignContent = _ref.alignContent,
      justify = _ref.justify,
      className = _ref.className,
      style = _ref.style,
      props = _objectWithoutPropertiesLoose(_ref, ["as", "direction", "reverse", "pad", "wrap", "grow", "inline", "flex", "align", "alignSelf", "alignContent", "justify", "className", "style"]);

  var combinedStyle = style || flex != null ? _extends({}, style, {
    flex: flex === true ? 1 : flex
  }) : style;
  return React.createElement(Component, _extends({}, props, {
    ref: ref,
    style: combinedStyle,
    className: classNames(className, direction && styles[camelCase(direction)], reverse && styles.reverse, pad && styles["pad" + (pad === true ? '3' : pad)], wrap && styles.wrap, grow && styles.grow, inline ? styles.flexInline : styles.flex, align && styles[camelCase("align-" + align)], alignSelf && styles[camelCase("self-" + alignSelf)], alignContent && styles[camelCase("align-content-" + alignContent)], justify && styles[camelCase("justify-" + justify)])
  }));
}

var DecoratedFlex = React.forwardRef(Flex);
DecoratedFlex.propTypes = propTypes;
DecoratedFlex.defaultProps = defaultProps;
export default DecoratedFlex;