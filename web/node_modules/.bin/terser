#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../next/node_modules/terser/bin/uglifyjs" "$@"
  ret=$?
else 
  node  "$basedir/../next/node_modules/terser/bin/uglifyjs" "$@"
  ret=$?
fi
exit $ret
