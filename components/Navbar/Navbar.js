import React from 'react';
import AppBar from '@material-ui/core/AppBar';
import Button from '@material-ui/core/Button';
import Toolbar from '@material-ui/core/Toolbar';

const navbar = () => (
  <AppBar>
    <Toolbar>
      <Button>My Alfred</Button>
    </Toolbar>
  </AppBar>
);

export default navbar;
